openapi: 3.0.0
info:
  title: Ping Pong
  version: 1.0.0
  description: Ecriture d'une API simple qui retourne Pong

paths:
  /ping:
    get:
      summary: Teste la disponibilité de l'API
      responses:
        '200':
          description: Pong !
          content:
            text/plain:
              schema:
                type: string
                example: pong
